# 🏗️ System Architecture

## Overview

```
┌─────────────────────────────────────────────────────────────┐
│                     MENSTRUAL MARKETPLACE                    │
│                   Health & Hygiene Platform                  │
└─────────────────────────────────────────────────────────────┘

┌──────────────────────┐         ┌──────────────────────┐
│   REACT FRONTEND     │◄───────►│   FASTAPI BACKEND    │
│   (Port 5173)        │  HTTP   │   (Port 8000)        │
│                      │  REST   │                      │
│  • Login UI          │   API   │  • Authentication    │
│  • Dashboards        │         │  • Business Logic    │
│  • Product Catalog   │         │  • Data Management   │
│  • Order Management  │         │  • Analytics         │
└──────────────────────┘         └──────────────────────┘
         │                                  │
         │                                  │
         ▼                                  ▼
┌──────────────────────┐         ┌──────────────────────┐
│   LOCAL STORAGE      │         │   IN-MEMORY DB       │
│  • JWT Tokens        │         │  • Users             │
│  • User Session      │         │  • Products          │
└──────────────────────┘         │  • Orders            │
                                 │  • Transactions      │
                                 └──────────────────────┘
```

## Component Architecture

### Frontend (React + Vite)

```
src/
├── components/
│   ├── Login.jsx              → Authentication UI
│   ├── BuyerDashboard.jsx     → Buyer interface
│   ├── SellerDashboard.jsx    → Seller interface
│   └── NGODashboard.jsx       → NGO analytics
│
├── services/
│   └── api.js                 → API integration layer
│
├── data/
│   └── mockData.js            → Demo data (can be removed)
│
└── App.jsx                    → Main routing
```

### Backend (FastAPI + Python)

```
backend/
└── main.py
    ├── Authentication Module
    │   ├── /api/auth/register
    │   ├── /api/auth/login
    │   └── /api/auth/me
    │
    ├── Products Module
    │   ├── /api/products (CRUD)
    │   └── Stock Management
    │
    ├── Orders Module
    │   ├── /api/orders (CRUD)
    │   └── Status Updates
    │
    ├── Transactions Module
    │   ├── /api/transactions
    │   └── /api/transactions/earnings
    │
    └── Analytics Module (NGO)
        ├── /api/analytics/platform-metrics
        └── /api/analytics/seller-performance
```

## Data Flow

### 1. Authentication Flow

```
┌──────┐     Login      ┌──────────┐    Verify     ┌──────────┐
│ User │───────────────►│ Frontend │──────────────►│ Backend  │
└──────┘                └──────────┘               └──────────┘
                              │                          │
                              │                          │
                              │      JWT Token           │
                              │◄─────────────────────────┘
                              │
                              ▼
                        ┌──────────┐
                        │  Local   │
                        │ Storage  │
                        └──────────┘
```

### 2. Product Browsing Flow

```
┌──────┐   View Products  ┌──────────┐   GET /products   ┌──────────┐
│Buyer │─────────────────►│ Frontend │──────────────────►│ Backend  │
└──────┘                  └──────────┘                   └──────────┘
                                │                              │
                                │        Products List         │
                                │◄─────────────────────────────┘
                                │
                                ▼
                          Display Catalog
```

### 3. Order Creation Flow

```
┌──────┐  Place Order  ┌──────────┐  POST /orders  ┌──────────┐
│Buyer │──────────────►│ Frontend │───────────────►│ Backend  │
└──────┘               └──────────┘                └──────────┘
                             │                          │
                             │                          ├─► Create Order
                             │                          ├─► Update Stock
                             │                          └─► Create Transaction
                             │                          │
                             │      Order Confirmation  │
                             │◄─────────────────────────┘
                             │
                             ▼
                       Success Message
```

### 4. Analytics Flow (NGO)

```
┌─────┐  View Analytics  ┌──────────┐  GET /analytics  ┌──────────┐
│ NGO │────────────────►│ Frontend │─────────────────►│ Backend  │
└─────┘                 └──────────┘                  └──────────┘
                              │                            │
                              │                            ├─► Calculate Metrics
                              │                            ├─► Aggregate Data
                              │                            └─► Format Response
                              │                            │
                              │      Platform Metrics      │
                              │◄───────────────────────────┘
                              │
                              ▼
                        Display Dashboard
```

## User Roles & Permissions

```
┌──────────────────────────────────────────────────────────┐
│                      USER ROLES                          │
└──────────────────────────────────────────────────────────┘

┌─────────────┐         ┌─────────────┐         ┌─────────────┐
│   SELLER    │         │    BUYER    │         │     NGO     │
├─────────────┤         ├─────────────┤         ├─────────────┤
│ • Create    │         │ • View      │         │ • View All  │
│   Products  │         │   Products  │         │   Orders    │
│ • Update    │         │ • Place     │         │ • Platform  │
│   Products  │         │   Orders    │         │   Metrics   │
│ • Delete    │         │ • View Own  │         │ • Seller    │
│   Products  │         │   Orders    │         │   Analytics │
│ • View      │         │             │         │ • All       │
│   Orders    │         │             │         │   Trans.    │
│ • Update    │         │             │         │             │
│   Status    │         │             │         │             │
│ • View      │         │             │         │             │
│   Earnings  │         │             │         │             │
└─────────────┘         └─────────────┘         └─────────────┘
```

## Security Architecture

```
┌─────────────────────────────────────────────────────────┐
│                   SECURITY LAYERS                        │
└─────────────────────────────────────────────────────────┘

1. Password Security
   ┌──────────────┐
   │  Plain Text  │
   └──────┬───────┘
          │ bcrypt hash
          ▼
   ┌──────────────┐
   │   Hashed     │
   │   Password   │
   └──────────────┘

2. JWT Authentication
   ┌──────────────┐
   │ User Login   │
   └──────┬───────┘
          │ Generate JWT
          ▼
   ┌──────────────┐
   │  JWT Token   │──► Stored in LocalStorage
   └──────┬───────┘
          │ Sent with requests
          ▼
   ┌──────────────┐
   │   Backend    │──► Verifies & Decodes
   │  Validation  │
   └──────────────┘

3. Role-Based Access Control (RBAC)
   ┌──────────────┐
   │   Request    │
   └──────┬───────┘
          │ Check JWT
          ▼
   ┌──────────────┐
   │ Extract Role │
   └──────┬───────┘
          │ Validate Permission
          ▼
   ┌──────────────┐
   │ Allow/Deny   │
   └──────────────┘
```

## API Request/Response Cycle

```
┌─────────────────────────────────────────────────────────┐
│              API REQUEST/RESPONSE CYCLE                  │
└─────────────────────────────────────────────────────────┘

Frontend                          Backend
   │                                 │
   │  1. User Action                 │
   ├─────────────────────────────────┤
   │                                 │
   │  2. API Call (api.js)          │
   │     • Add JWT Token             │
   │     • Set Headers               │
   ├────────────────────────────────►│
   │                                 │
   │                                 │  3. Validate Token
   │                                 │     • Decode JWT
   │                                 │     • Check Expiry
   │                                 │
   │                                 │  4. Validate Permissions
   │                                 │     • Check Role
   │                                 │     • Verify Access
   │                                 │
   │                                 │  5. Process Request
   │                                 │     • Business Logic
   │                                 │     • Data Operations
   │                                 │
   │  6. Response                    │
   │◄────────────────────────────────┤
   │     • JSON Data                 │
   │     • Status Code               │
   │                                 │
   │  7. Update UI                   │
   │     • Display Data              │
   │     • Show Messages             │
   │                                 │
```

## Database Schema (Future)

```
┌─────────────────────────────────────────────────────────┐
│                  DATABASE SCHEMA                         │
└─────────────────────────────────────────────────────────┘

┌──────────────┐         ┌──────────────┐
│    Users     │         │   Products   │
├──────────────┤         ├──────────────┤
│ id (PK)      │         │ id (PK)      │
│ phone        │         │ name         │
│ password     │         │ category     │
│ name         │◄────────┤ seller_id(FK)│
│ role         │   1:N   │ price        │
│ village      │         │ stock        │
│ state        │         │ description  │
│ created_at   │         │ created_at   │
└──────┬───────┘         └──────┬───────┘
       │                        │
       │ 1:N                    │ 1:N
       │                        │
       ▼                        ▼
┌──────────────┐         ┌──────────────┐
│    Orders    │         │ Transactions │
├──────────────┤         ├──────────────┤
│ id (PK)      │────────►│ id (PK)      │
│ order_number │   1:1   │ order_id(FK) │
│ buyer_id(FK) │         │ seller_id(FK)│
│ seller_id(FK)│         │ amount       │
│ product_id   │         │ platform_fee │
│ quantity     │         │ net_earning  │
│ total_amount │         │ status       │
│ status       │         │ created_at   │
│ order_date   │         └──────────────┘
└──────────────┘
```

## Technology Stack

```
┌─────────────────────────────────────────────────────────┐
│                   TECHNOLOGY STACK                       │
└─────────────────────────────────────────────────────────┘

Frontend:
  • React 18
  • Vite
  • React Router
  • Lucide Icons
  • CSS3

Backend:
  • FastAPI
  • Python 3.11+
  • Uvicorn (ASGI)
  • Pydantic
  • JWT (python-jose)
  • Passlib (bcrypt)

Future:
  • PostgreSQL / MySQL
  • Redis (Caching)
  • Docker
  • Nginx
```

## Deployment Architecture (Future)

```
┌─────────────────────────────────────────────────────────┐
│              PRODUCTION DEPLOYMENT                       │
└─────────────────────────────────────────────────────────┘

                    ┌──────────────┐
                    │   Internet   │
                    └──────┬───────┘
                           │
                           ▼
                    ┌──────────────┐
                    │  Load        │
                    │  Balancer    │
                    └──────┬───────┘
                           │
              ┌────────────┼────────────┐
              │                         │
              ▼                         ▼
       ┌──────────────┐         ┌──────────────┐
       │   Frontend   │         │   Backend    │
       │   (Vercel/   │         │   (Heroku/   │
       │   Netlify)   │         │   AWS/DO)    │
       └──────────────┘         └──────┬───────┘
                                       │
                                       ▼
                                ┌──────────────┐
                                │  PostgreSQL  │
                                │   Database   │
                                └──────────────┘
```

## Key Features Summary

✅ **18 API Endpoints** - Complete REST API
✅ **JWT Authentication** - Secure token-based auth
✅ **Role-Based Access** - Seller, Buyer, NGO roles
✅ **Product Management** - Full CRUD operations
✅ **Order Processing** - Complete order lifecycle
✅ **Transaction Tracking** - Automatic financial records
✅ **Analytics Dashboard** - Platform metrics for NGOs
✅ **Interactive Docs** - Swagger UI & ReDoc
✅ **Frontend Service** - Ready-to-use API integration
✅ **Demo Data** - Pre-loaded test accounts

---

**Architecture designed for scalability and maintainability** 🚀

